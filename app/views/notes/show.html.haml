.wrapper_with_padding 
  #note_show 
    %h1.title= @note.title 
    %P= @note.content

  .buttons 
    = link_to "編集する", edit_note_path(@note), class: "button"
    = link_to "削除する", note_path(@note), method: :delete, data: { confirm: "本当に削除してもよろしいですか？" }, class: "button"